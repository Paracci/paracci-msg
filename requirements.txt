# ── Runtime dependencies ───────────────────────────────────────────────────
# Flask web server (serves the UI via pywebview)
Flask>=3.0.0
# pywebview — native desktop window wrapping the Flask app
pywebview>=5.0.0
# Cryptography primitives
cryptography>=42.0.0
# Password hashing (Argon2)
argon2-cffi>=23.1.0
# TOTP / 2FA
pyotp>=2.9.0
# QR code generation
qrcode>=7.4.2
# Image processing (qrcode dependency)
Pillow>=10.0.0
# Dependency version parsing
packaging
# Security audit tool (dev/CI only — safe to include)
pip-audit
